import 'package:flutter/material.dart';

class UserPendingDetails extends StatelessWidget {
  const UserPendingDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}